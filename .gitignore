# Build artifacts (anchored to repo root — `prism-bridge` unanchored would
# also match cmd/prism-bridge/ which is real source)
bin/
/mcpgate
/prism
/prism-bridge
/prism-auth
*.exe

# Local config and secrets — never commit
config.json
.env
auth.json

# Per-machine / per-tool runtime dirs
.codex/
.opencode/
.recoil/
.familiar/
.hallmark/
.playwright-cli/
.vscode/
.idea/
opencode.json

# Internal task tracking / design scratch — not for public distribution
.brainfile/
design.md
docs/superpowers/

# Screenshots and design refs — never committed; reference them from PRs instead
*.png
*.jpg
*.jpeg
*.gif
*.webp

# Local-only Claude Code permissions (settings.json is team-shared)
.claude/settings.local.json

# Scratch / leftover dev scripts
test_tool.sh
